# Database Material

Shared database planning belongs here. The active Prisma schema remains in `7Ink-Dashboard/prisma` until another application consumes the same models.

When extracting shared database material, keep migrations versioned and document the owning runtime for every model.
