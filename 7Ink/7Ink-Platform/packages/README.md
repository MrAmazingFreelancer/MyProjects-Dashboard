# Shared Packages

Add a package here only when it is consumed by at least two 7Ink applications.

Potential package boundaries include:

- `types`: shared API and domain types
- `validation`: shared request and form schemas
- `ui`: shared visual components and design tokens
- `config`: shared linting, TypeScript, or build configuration
